// Test stub for `$app/environment`. Vite resolves the real module from
// SvelteKit's runtime; for unit tests we just need plausible values.
export const browser = true;
export const dev = true;
export const building = false;
export const version = 'test';
